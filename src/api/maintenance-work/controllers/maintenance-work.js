'use strict';

/**
 *  maintenance-work controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::maintenance-work.maintenance-work');
